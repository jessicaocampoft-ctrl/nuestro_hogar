import React from 'react';import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';import { createNativeStackNavigator } from '@react-navigation/native-stack';import { Ionicons } from '@expo/vector-icons';import { HomeScreen } from '../screens/HomeScreen';import { ExpensesScreen } from '../screens/ExpensesScreen';import { BalanceScreen } from '../screens/BalanceScreen';import { TasksScreen } from '../screens/TasksScreen';import { NotesScreen } from '../screens/NotesScreen';import { ProfileScreen } from '../screens/ProfileScreen';import { ClosureScreen } from '../screens/ClosureScreen';import { colors } from '../theme';
const Tab=createBottomTabNavigator(),Stack=createNativeStackNavigator();
const icons:Record<string,keyof typeof Ionicons.glyphMap>={Inicio:'home-outline',Gastos:'receipt-outline',Balance:'scale-outline',Tareas:'checkmark-circle-outline',Notas:'heart-outline',Perfil:'person-outline'};
function Tabs(){return <Tab.Navigator screenOptions={({route})=>({headerShown:false,tabBarActiveTintColor:colors.primary,tabBarInactiveTintColor:colors.muted,tabBarLabelStyle:{fontSize:11,fontWeight:'700'},tabBarStyle:{height:70,paddingTop:7,paddingBottom:8,backgroundColor:colors.surface,borderTopColor:colors.border},tabBarIcon:({color,size})=><Ionicons name={icons[route.name]} color={color} size={size}/>})}><Tab.Screen name="Inicio" component={HomeScreen}/><Tab.Screen name="Gastos" component={ExpensesScreen}/><Tab.Screen name="Balance" component={BalanceScreen}/><Tab.Screen name="Tareas" component={TasksScreen}/><Tab.Screen name="Notas" component={NotesScreen}/><Tab.Screen name="Perfil" component={ProfileScreen}/></Tab.Navigator>}
export function AppNavigator(){return <Stack.Navigator screenOptions={{headerShown:false}}><Stack.Screen name="Principal" component={Tabs}/><Stack.Screen name="Cierre" component={ClosureScreen}/></Stack.Navigator>}
