import React from 'react';import { Alert,Text } from 'react-native';import { Screen } from '../components/layout';import { Card,PrimaryButton,SectionHeader,ui } from '../components/ui';import { useApp } from '../hooks/useApp';import { supabase } from '../services/supabase';import { colors } from '../theme';
export function ProfileScreen({navigation}:any){const a=useApp();return <Screen title="Perfil" subtitle="Tu cuenta y su hogar compartido."><Card style={{gap:6}}><Text style={ui.section}>Jessica</Text><Text style={ui.muted}>jessica@hogar.app</Text></Card><SectionHeader title="Nuestro hogar"/><Card style={{gap:6,backgroundColor:colors.primarySoft}}><Text style={ui.section}>Jessica & Mateo</Text><Text style={ui.muted}>Código de invitación</Text><Text selectable style={[ui.section,{letterSpacing:4}]}>HOGAR2</Text><Text style={ui.muted}>2 de 2 integrantes</Text></Card><PrimaryButton label="Ver cierre mensual" icon="calendar-outline" onPress={()=>navigation.navigate('Cierre')}/><PrimaryButton label="Cerrar sesión" variant="soft" icon="log-out-outline" onPress={async()=>{await supabase.auth.signOut();Alert.alert('Sesión cerrada','En modo de muestra puedes reiniciar la app para volver al acceso.')}}/></Screen>}
